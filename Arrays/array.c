#include <stdio.h>

int main(){
        //define an array
        int numbers [10];

        //populate array
        numbers[0] = 110;
        numbers[1] = 100;
        numbers[2] = 90;
        numbers[3] = 80;
        numbers[4] = 70;
        numbers[5] = 80;
        
        //have to specify that a number is being printed
        printf("The second place in the array is %d\n", numbers[1]);


        return 0;
}

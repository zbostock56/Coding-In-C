#include <stdio.h>

int main() {
        //standard integer variable initialization
        int foo = 1;
        int bar;

        //doing some integer addition
        int a, b = 80, c = 31, d = 14;
        a = b - c - d;
        printf("%d\n", a);

        //basically means that the code compiled with no errors
        return 0;
}
